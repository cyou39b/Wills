using UnityEngine;
using UnityEngine.UI;

public class PossessionLogic : MonoBehaviour{
    public void Initialize(){
        //PossessionItems ThisPossession;
        /*(gameObject.transform.Find("Image").TryGetComponent<Image>(out Image img)){
            img.sprite = ThisPossession.sprite;
        }
        if(gameObject.transform.Find("Text").TryGetComponent<Text>(out Text txt)){
            txt.text = ThisPossession.Name;
        }*/
    }
}